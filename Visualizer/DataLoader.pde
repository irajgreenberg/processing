// handles loading and basic formatting of data
class DataLoader {
  String url;
  DataLoader() {
  }
  DataLoader(String url) {
    this.url = url;
  }
  
  JSONObject getJSON() {
  }
  getXML
  gteCSV()
}